# ---- Core API -----------------------------------------------------------
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0
sqlalchemy[asyncio]>=2.0.23
asyncpg>=0.29.0
aiosqlite>=0.19.0
alembic>=1.13.0

# ---- Production server --------------------------------------------------
gunicorn>=21.2.0

# ---- Cache / Pub-Sub ----------------------------------------------------
redis>=5.0.1
fakeredis>=2.20.0

# ---- Async job queue ----------------------------------------------------
celery[redis]>=5.3.6
kombu>=5.3.3

# ---- Auth ---------------------------------------------------------------
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6

# ---- ML / ONNX ----------------------------------------------------------
onnxruntime>=1.17.0         # CPU inference; swap for onnxruntime-gpu on GPU nodes
numpy>=1.26.0
transformers>=4.36.0
optimum[onnxruntime]>=1.16.0
tokenizers>=0.15.0

# ---- STT (local Whisper — no paid APIs) ----------------------------------
openai-whisper>=20231117
ffmpeg-python>=0.2.0

# ---- Observability ------------------------------------------------------
structlog>=24.1.0
prometheus-client>=0.20.0
starlette-prometheus>=0.9.0

# ---- Distributed tracing (OpenTelemetry) --------------------------------
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-instrumentation-fastapi>=0.43b0
opentelemetry-instrumentation-sqlalchemy>=0.43b0
opentelemetry-exporter-otlp-proto-grpc>=1.22.0

# ---- Resilience ---------------------------------------------------------
pybreaker>=1.2.0
tenacity>=8.2.3             # exponential backoff for DB/Redis retries

# ---- System monitoring --------------------------------------------------
psutil>=5.9.8               # CPU / memory metrics for load shedding

# ---- Security -----------------------------------------------------------
slowapi>=0.1.9
twilio>=9.0.0

# ---- Dashboard ----------------------------------------------------------
jinja2>=3.1.0
aiofiles>=23.0.0

# ---- Testing ------------------------------------------------------------
pytest>=7.4.3
pytest-asyncio>=0.21.1
httpx>=0.25.2
respx>=0.20.0
groq>=0.4.2
pydub>=0.25.1
torch>=2.1.0
torchaudio>=2.1.0
soundfile>=0.12.1

# ---- Load testing -------------------------------------------------------
locust>=2.20.0
